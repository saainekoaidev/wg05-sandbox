# 要件定義

US-001	ユーザとして, メールアドレスとパスワードでアカウントを新規登録したい。なぜなら自分専用に通勤経路を保存したいから。
US-002	ユーザとして, メールアドレスとパスワードでログインしたい。なぜなら登録済みの経路を確認･編集したいから。
US-003	ユーザとして, 出発駅と到着駅と経由駅と区間運賃を入力して通勤経路を登録したい。なぜなら毎月の交通費を可視化したいから。
US-004	ユーザとして, 自分が登録した通勤経路を一覧で見たい。なぜなら登録済みの経路をひと目で把握したいから。
US-005	ユーザとして, 1経路の詳細情報を見たい。なぜなら経由や区間ごとの内訳を確認したいから。
US-006	ユーザとして, 既存の通勤経路を編集したい。なぜなら住所変更等で経路が変わった場合に更新したいから。
US-007	ユーザとして, 不要になった通勤経路を削除したい。なぜなら過去の不要データを整理したいから。
US-008	ユーザとして, 自分の登録済みプロフィール (氏名/郵便番号) を変更したい。なぜなら引っ越しや改姓などのライフイベントで情報が変わるから。
US-009	ユーザとして, 自分のパスワードを変更したい。なぜなら定期的なパスワード更改や情報漏洩懸念時に自衛したいから。
US-010	ユーザとして, 自分のアカウントを削除したい。なぜならサービスを利用しなくなった際に登録情報を残したくないから。
US-011	システムとして, 東海4県 (愛知/岐阜/三重/静岡) の鉄道路線および駅マスタを取り込んでおきたい。なぜなら本サンドボックスが想定する利用エリアの経路を網羅的に登録できるようにするため。
US-012	管理者として, 路線マスタを画面から登録/修正/削除したい。なぜなら US-011 の取り込み後に実態と乖離した路線情報を運用で補正したいから。
US-013	管理者として, 駅マスタおよび駅↔路線の接続情報を画面から登録/修正/削除したい。なぜなら新駅開業/廃駅/接続変更を反映したいから。
US-014	ユーザとして, ヘッダ/フッタなど自分の識別を示す箇所には登録した氏名 (User.name) を表示してほしい。なぜならメールアドレスは ID 的な意味合いが強く、ログイン中アカウントを直感的に確認したい場面では氏名のほうが自然なため。
US-015	ユーザとして, プロフィール設定画面 (S08) のパスワード変更欄および削除確認用パスワード欄に, 画面表示時にブラウザの自動補完で値が埋まっていないでほしい。なぜなら入力していないパスワードが表示されているのは違和感があり, 誤操作 (そのまま「変更する」「削除する」を押下) の温床にもなるため。
US-016	ユーザとして, 経路登録/編集 (S04 / S06) の各区間で「種別」「路線」を選択した状態で「駅選択」ボタンを押下した時, 駅マスタ参照 (S07) が選択済み条件で絞り込んだ検索結果を最初から表示している状態で開いてほしい。なぜなら毎回同じ条件を popup 側で入力し直すのが煩わしいため。
US-017	ユーザとして, 駅マスタ参照 (S07) の検索フォームで種別を選択した時に路線リストの選択肢がその種別の路線のみに絞られ, 逆に路線を選択した時に種別もその路線が属する種別に自動で揃うようにしてほしい。なぜなら矛盾した条件 (例: 種別=バスで地下鉄路線) で空振り検索することを防ぐため。
US-018	システムとして, JR が運営する路線の表示名は「JR○○線」「JR○○本線」のように "JR" プレフィックスを付けて表示したい。なぜなら Wikidata 由来の素の名称 (中央本線, 関西本線, 等) では他事業者線と区別しづらいため。
US-019	ユーザとして, ログイン中の自分の氏名 (User.name) をログイン/サインアップ画面以外のすべての画面でヘッダ右上に常時表示してほしい。なぜなら現在ログインしているアカウントを画面に関わらず即座に確認したいから。「ユーザー：」のラベルは不要で氏名のみ表示し、クリックでアカウント設定 (/account) に遷移することで US-014 の動線を維持する。
US-020	ユーザとして, 経路登録 (S04) / 経路編集 (S06) の各区間でも、種別を選択すると路線セレクトの選択肢が該当種別のものだけに絞られ, 路線を選択すると種別がその路線の種別に揃う cascade 連動が効いてほしい。なぜなら US-017 で駅マスタ参照画面に入れた仕組みと同じ整合性を経路入力でも維持したいから。
US-021	ユーザとして, 経路一覧 (S03) のヘッダで右上の UserBadge と「+ 新規登録」ボタンが視覚的に重ならないようにレイアウトを調整してほしい。なぜなら現状ボタンとバッジが重なって読みにくいから。
US-022	ユーザとして, 経路詳細 (S05) のカード幅を広げ、「一覧に戻る」ボタンのラベルが改行されないようにしてほしい。「編集」「削除」ボタンの幅も詰まりすぎないように適度に広げ、画面が縦長すぎず読みやすくなるようにしたい。
US-023	システムとして, 駅マスタの kana 列に漢字駅名 (フォールバック値) が混入しないようにしてほしい。Wikidata に kana 情報が無い駅は kana を空にする。なぜなら検索画面で kana が漢字表記で出るのは利用者に違和感を与えるから (本格的な漢字→ひらがな変換は別途検討)。
US-024	ユーザとして, プロフィール設定 (S08) の ADMIN セクションの説明文 (「管理者として以下のマスタ管理画面にアクセスできます」) を他のセクションの hint と同じ位置 (左インデント) に揃え, マスタ管理ボタン 2 つを少しコンパクトにしてほしい。なぜなら現状はテキストの位置が左端に寄りすぎて違和感があり, ボタンも横幅が広すぎてセクションのバランスが崩れているから。
US-025	管理者として, 路線マスタ管理 (S09) の新規作成と編集を専用画面 (URL) で行いたい。なぜなら現状のインライン展開フォームよりも独立画面のほうが入力に集中しやすく, 戻る操作も明示的に行えるから (新規 = `/admin/lines/new`, 編集 = `/admin/lines/:id/edit`)。
US-026	管理者として, 駅マスタ管理 (S10) の新規作成と編集を専用画面 (URL) で行いたい。理由は US-025 と同じ (新規 = `/admin/stations/new`, 編集 = `/admin/stations/:id/edit`)。
US-027	システムとして, US-011 で取り込んだ駅マスタの大部分で kana が空になっているところに自動でひらがなを入れたい。なぜなら駅マスタ参照のかな検索 (US-003 想定) が空欄駅では効かず利用体験が低下するから。Wikidata に kana が設定されている駅はそれを使い、無い駅は駅名漢字を形態素解析等でひらがな化して埋める。末尾の「えき」は付けない。
US-028	ユーザとして, 駅マスタ管理 (S10) および路線マスタ管理 (S09) の操作列で「編集」と「削除」ボタンが行高内で横並びになるようにしてほしい。なぜなら現状はセル幅が狭いと縦に折り返されて見栄えが悪いから (`.col-actions` の flex-wrap を nowrap に変更)。
US-029	ユーザとして, 路線マスタ管理 (S09) / 駅マスタ管理 (S10) のフッタから「経路一覧へ」リンクは取り除いてよい。なぜならアカウント設定経由でも経路一覧に戻れるため重複動線である。フッタは「アカウント設定」のみ残す。
US-030	システムとして, 駅マスタに駅番号 (Wikidata の P296 = station code) を取り込み, 駅マスタ参照画面 (S07) で「種別」「よみがな」「駅番号」のいずれかで昇順/降順ソートできるようにしたい。なぜなら実際の駅の並び順 (路線上の駅番号順) で確認したい場面があるから。
US-031	管理者として, 路線マスタ管理 (S09) の一覧を種別 (電車/地下鉄/バス/その他) で絞り込みたい。なぜなら登録路線数が増えるとリストが長くなり, 見たい種別だけ確認しづらいから。
US-032	管理者として, 駅マスタ管理 (S10) の一覧を種別 + 路線で絞り込みたい。なぜなら US-011 取り込み後は 800 駅超で一覧が膨大になり, 編集対象の駅にたどり着くのに時間がかかるから。種別は cascade で路線セレクトを絞り (US-017 と同じ挙動), 路線も種別もすべて で全件表示。
US-033	管理者として, 駅マスタ管理 (S10) の新規/編集画面で各駅に「路線ごとの駅番号」を表示・入力したい。なぜなら駅番号は実体として路線ごとに採番される (例: 名古屋駅は東海道線で CA68, 中央線で CC00) ものであり, 駅単位の単一フィールドでは正しく表現できないから。データ構造として `Station.code` は廃止し, `StationLine.code` を新設する。UI では接続路線セレクト (チェックボックス) の各行に駅番号入力枠を同居させ, チェックを外すと駅番号入力もクリアされる。チェック列の幅はチェックボックス本体に合わせて狭くする。
US-034	管理者として, 路線マスタ管理 (S09) と 駅マスタ管理 (S10) の絞り込みフィルタ (種別 / 路線) を新規・編集画面に遷移して戻ってきたときに維持しておいてほしい。なぜなら US-031/032 で追加したフィルタは画面遷移ごとに初期化されてしまい, 同じ駅/路線群を続けて編集する作業フローで毎回フィルタを掛け直す必要があるから。戻ってきたときにはその維持されたフィルタ条件で一覧を再取得して最新状態を表示する。永続範囲はセッション (sessionStorage) で十分。
US-035	管理者として, 駅の新規登録・編集画面の「接続路線 / 駅番号」リストで, 路線名が縦に折り返されず 1 行で表示されるようにしてほしい。なぜなら現状は駅番号入力枠が広すぎて路線名側の幅が狭まり「上飯田線」が「上 / 飯 / 田 / 線」のように 1 文字ずつ縦並びになり読みづらいから。駅番号入力枠を狭めれば路線名 1 行表示にできる。
US-036	管理者として, 駅マスタ管理 (S10) の一覧表で駅名・よみがなを 1 行で表示してほしい。行の高さが広がる要因は接続路線/駅番号の数が 1 行に収まらない場合だけにしたい。なぜなら現状は接続路線セルの幅が広く確保されすぎて, 同じ行の駅名・よみがなが影響を受け縦長になることがあるから。
US-037	システムとして, 駅マスタ取り込み時に「駅番号」(例: CA68) と「電報略号」(例: カカ, オキ, ヲキ) を区別し, 電報略号は StationLine.code に含めないでほしい。なぜなら Wikidata の P296 (station code) には JR 駅では駅番号と電報略号 (カタカナの古い識別子) が混在しており, 取り込み後の駅番号入力欄に電報略号が紛れ込むと, 画面上で駅番号バリデーション (US-033 の半角英数+ハイフン/スラッシュ) を通らず編集できないから。判定は「半角英数 + ハイフン/スラッシュ/ドット/アンダースコアのみで構成される値のみ採用」とし, それ以外 (カタカナ/ひらがな/漢字を含む値) は除外する。
US-038	管理者として, 駅マスタ管理の新規/編集 (および路線マスタ管理の新規/編集) でフォーム送信時にバリデーションエラーが発生したら, エラーの原因となった入力枠やドロップダウンを視覚的に強調表示し, 自動でその位置にスクロール + フォーカスしてほしい。なぜなら現状はエラーメッセージがフォーム上部のバナーにのみ出るため, 該当の入力欄が画面外にあるときに「どこを直せば良いか」を探す必要があるから。実装は対象 input/select に `aria-invalid="true"` + `.is-error` クラスを付与し, CSS で red border + ring を出す。
US-039	システムとして, 駅マスタ取り込み時に複数路線が乗り入れる駅で「駅番号が路線をまたいで併記」されないようにしてほしい。なぜなら駅番号は実体として 1 駅×1 路線 に対して 1 つだけ採番されるものであり (例: 千種 = JR中央本線で CF03, 地下鉄東山線で H12), 現行の import は qualifier 無しの P296 値を「全 lineLink に同じ値を流し込む」fallback で処理するため複数路線駅で `CF03/H12` のように両路線に同じ併記が入ってしまうから。修正方針: SPARQL クエリに qualifier `P518` (applies to part) を加えて Wikidata の路線対応 qualifier を漏れなく取得 + 「未埋め lineLink が 1 件 + unattached code 1 件」のケースで残り 1 lineLink に自動割当する補完ロジックを入れる。残ったあいまいケース (未埋め lineLink ≥ 2 + unattached ≥ 1) は自動採用しない。複数路線駅の手動補完は原則不要。
US-040	システムとして, US-039 の補完では救えなかった「両 unattached + 両 unfilled」の駅 (例: 大曽根 = `CF04` + `ST06`) でも, 駅番号の prefix (英字部分) から路線を推定して自動割当してほしい。なぜなら `CF04` の prefix `CF` は中央本線記号, `ST06` の `ST` は名鉄瀬戸線の記号で、人間には自明に区別がつくのに自動取り込みで空のままになるのは捜索が甘いから。実装方針: 取り込み中の SPARQL 結果から qualifier 付きで紐付いた code を学習データとして「路線 → prefix 集合」を動的構築し, unattached code はその prefix で該当 lineLink に割当する。
US-041	システムとして, Wikidata 上で「同じ物理駅が複数の Q-ID に分割されている」(例: 大曽根駅 = Q872075 (JR/名鉄) + Q110831202 (地下鉄名城線), 名古屋駅 = 5 エンティティに分割) ケースを駅マスタ取り込み時に 1 つの Station に統合してほしい。なぜなら現状は駅マスタ管理 (S10) や 駅マスタ参照 (S07) で「大曽根」が 2 行に分かれて表示され, 利用者が経路登録 (S04) で駅を選ぶ時に「どの大曽根を選ぶべきか」迷うから。マージ条件: (a) 一方が他方を P138 (名前の由来) で参照している, または (b) 同名 + 座標距離 < 500m。canonical Q-ID は P138 で参照されている方 (= 元エンティティ) を優先し、そうでなければ lineLink 数が最大のもの。
US-042	システムとして, Wikidata で同一路線が複数 Q-ID で表現されているケース (例: 「東海道本線 全国 (Q1190152)」と「東海道線 名古屋地区 (Q11235139)」+「東海道線 静岡地区 (Q11527981)」が階層関係) において, 駅エンティティが地区版ではなく親エンティティ (Q1190152) にしか紐付いていない場合 (例: 金山駅 Q124429659 は P81=Q1190152 のみ) でもその駅を取り込み対象路線に正しく割り当ててほしい。なぜなら現状は P81 が JR_LINE_QIDS の Q-ID と完全一致しないと駅が取り込まれず, 結果として金山駅の JR東海道線接続 (駅番号 CA66) が抜け落ちているなど網羅性に欠けるから。実装方針: 取り込み対象路線ごとに「エイリアス Q-ID 集合」を明示マップで持ち, 駅の P81 値がエイリアス Q-ID なら canonical 路線として正規化する。1 つのエイリアスが複数 canonical 路線にマップされる場合 (Q1190152 → 静岡 or 名古屋) は駅座標 (経度) で disambiguation する。
US-043	(撤回) システムとして, 全路線の駅の網羅性と駅番号の完全付番を自動で実現してほしい。動的発見部分は ADR 0007 スコープを承認なく拡張したためレビューで撤回された。詳細は ADR 0014 (Superseded) と ADR 0015 を参照。本 US の意図は US-044 で限定的に再定義された。
US-044	システムとして, ADR 0007 で凍結したスコープ (JR 14 路線 + OTHER_OPERATORS 5 社) の範囲内で駅番号の付番率を最大化してほしい。具体的には: (a) `fetchStationsForLines` のマージ前 (ADR 0012 適用前) に, lineLink が単独な Q-ID については unattached code を当該 lineLink に確定する変換を入れる。これにより, あおなみ線名古屋駅 (Q124417968 = lineLink {あおなみ線} + unattached {AN01}) のような単独路線 Q-ID がマージで JR名古屋駅 (Q110799384) に吸収される際に AN01 が ambiguous でスキップされず, 名古屋駅で「あおなみ線=AN01」が保持される。(b) 取り込み終了時に駅番号未付番の lineLink を `docs/audit/missing-station-codes.md` に出力し, Wikidata 側のデータ不備で自動補完できない駅を可視化する。ADR 0014/0015 で文書化。
US-045	システムとして, 駅マスタ取り込みのデータソースを Wikidata 単独から「Wikidata + 国土数値情報 N02 (国土交通省 国土政策局)」併用に拡張してほしい。Wikidata は駅番号 (P296) や路線対応 qualifier の構造化情報を持つため引き続き主ソースとし, N02 は路線網・駅・座標の網羅性を補完する従ソースと位置付ける。N02 GeoJSON を 4 県分ダウンロードして, Wikidata 由来駅と「座標距離 < 200m + 名称一致」で突合し, 一致した駅は N02 の運営会社・正式路線名で補完する。Wikidata に無い駅は新規追加する (sourceUri に国土数値情報 URL を記録)。これにより US-044 の audit レポートで残った未付番駅・未取り込み駅をある程度カバーできる見込み。なお駅番号自体は N02 に含まれないため, 駅番号の追加補完源 (例: Wikipedia 駅ナンバリングページ) は将来別 US で検討する。ADR 0016 で文書化。**US-047 で修正**: N02 取り込みは US-046 (経路自動取得) の先行基盤として位置付け直し, デフォルト無効化 (`--with-n02` フラグで有効化)。
US-046	(将来予定) ユーザとして, 自宅と勤務先の郵便番号もしくは住所を入力すると, 最寄り駅の自動抽出 + 経路①(最短) + 経路②(利便性考慮) を自動算出して通勤経路として登録できる機能がほしい。Google Maps API などの外部経路探索 API を利用する想定。前提として座標付き駅マスタが必要であり, US-045 の N02 取り込み (`--with-n02` 有効化) で整備される。実装は別途新規 ADR で詳細化する。
US-047	システムとして, US-045 の N02 取り込みは US-046 用の先行基盤と位置付けデフォルト無効化し, 既存の N02 由来駅 (266 件) も DB から削除する仕組みを用意してほしい。並行して駅番号の網羅性を本格的に補完する手段として Wikipedia 駅ナンバリングページ (https://ja.wikipedia.org/wiki/%E9%A7%85%E3%83%8A%E3%83%B3%E3%83%90%E3%83%AA%E3%83%B3%E3%82%B0) を MediaWiki API で取得し, スコープ内 (JR_LINE_QIDS + OTHER_OPERATORS) の駅で qualifier 付きで取れていなかった駅番号を補完する。並行して audit レポートをソース別 (Wikidata / N02 / Wikipedia) に分離して付番率を正しく可視化する。ADR 0017 で文書化。
US-048	システムとして, US-047 の駅ナンバリングページ取り込みでは始終点・主要駅のみで全駅補完できなかったため (例: 名古屋駅の JR東海道線 = CA68 が取れない), 取り込み対象 Line ごとに **個別の Wikipedia 路線記事** を Wikidata Q-ID 経由で取得し, 駅一覧表 (wikitable) をパースして駅番号を全駅補完してほしい。MediaWiki API + 7 日キャッシュ。すべての駅番号空 lineLink (路線×駅) について同じ補完処理を適用する。Wikidata 既存値は上書きしない (qualifier 値が信頼できる)。残った未付番は信号場・廃駅・誤接続等の構造的に駅番号がないものに限定される。ADR 0018 で文書化。
US-049	管理者として, 路線の所属先を表す「運営会社マスタ」(Operator) を独立したマスタとして管理したい。なぜなら現状は `Line.operator` が単なる文字列フィールドで, 同名駅であっても運営会社が違えば別駅と扱うべき (= 改札を出ての乗換えになる) という本質的なルールを表現できておらず, 駅番号取得や経路登録の分かりやすさにも影響しているため。基準: 運賃が連続する (= 改札内の乗換え) 範囲が同一駅, 改札を出る場合は別駅。例えば JR東海の東海道線↔関西本線は同一駅 (1 レコード), JR東海名古屋駅↔名鉄名古屋駅は別駅 (2 レコード)。実装内容: (a) Operator マスタ (id, name, aliases) と管理画面 (S11 一覧 / 新規 / 編集 / 削除), (b) `Line.operatorId` FK 追加 (互換のため `Line.operator` 文字列は当面維持), (c) `Station.operatorId` FK 追加 (各 Station は単一 operator に属する), (d) 既存 `Line.operator` 文字列から Operator 6 社を初期データとして自動生成 + Line.operatorId 設定, (e) Wikidata 取り込みで複数 operator にまたがる Q-ID (大曽根 Q872075 の JR + 名鉄等) は operator 別に Station レコードを分割, (f) US-041 の merge ロジックを「同一 operator 内のみ merge」に制限, (g) AdminLineForm の運営会社入力を Operator ドロップダウン化, AdminStations 一覧に operator フィルタ追加。ADR 0019 で文書化。
US-050	ユーザ・管理者として, 運営会社・種別・路線のセレクト枠が並ぶすべての画面 (経路登録/編集 S04/S06, 駅マスタ参照 S07, 路線マスタ管理 S09, 駅マスタ管理 S10, 路線マスタ新規/編集 S09 サブ画面, 駅マスタ新規/編集 S10 サブ画面) で「運営会社 → 種別 → 路線」の左→右並びに統一してほしい。さらに 3 セレクトの相互連動を以下の規則で実装する: (a) 運営会社を選ぶと路線セレクトが該当 operator の路線のみに絞り込まれる (種別セレクト自体は 4 件しかないため絞らず常に全選択肢を表示), (b) 種別を選ぶと候補 operator が 1 社に確定する場合は運営会社を自動選択 (例: subway → 名古屋市交通局), 路線セレクトは該当 kind に絞り込み, (c) 路線を選ぶと運営会社と種別がその路線の値に hard-set される。3 軸の相互ルールは `frontend/src/lib/cascade.ts` に共通実装し各画面で再利用する。さらに `/api/stations` エンドポイントに `?operator=xxx` パラメタ + Station 応答に `operatorName` を追加する。
US-062	ユーザとして, (a) 駅マスタ参照 (S07) は popup ダイアログとして開かれるためプロフィール編集 (UserBadge) リンクを掲出しないでほしい — popup 内にユーザ識別を出す必要は無い。(b) 経路一覧 (S03) のテーブルは「# / 経路名 / 種別 / 出発→到着 / 区間 / 合計運賃 / ボタン3つ」が 1 行になる構成とし, 路線サマリ列・運営会社列を撤去してほしい。
US-061	ユーザとして, ログアウトを手軽にできるようにしてほしい。ただし全画面にログアウトを設けるのは望ましくない。プロフィール編集 (S08) は UserBadge からどの画面でも遷移できるので, プロフィール編集にログアウトボタンを集約する。経路一覧 (S03) のフッタにあるログアウトリンクは削除する。
US-060	ユーザとして, 経路詳細 (S05) の Segments 行で「出発駅 → 到着駅」が文字単位で改行されてしまう (例: 「中津川→いんこ」が縦に 1 文字ずつ折り返される) のを必ず 1 行表示で固定してほしい。原因: `.seg-flow` が `flex: 1` で空きスペースが足りなくなった時に shrink → 文字単位で wrap していた。修正: `.seg-flow` と `.seg-line` に `white-space: nowrap` を付与し, `.seg-flow` の flex を `0 0 auto` (shrink しない) に変更。
US-059	ユーザとして, 経路登録/編集で選択した運営会社を経路詳細にも表示してほしい (現状 segment.operator は UI 専用で永続化されていないため編集画面で再選択が必要だった)。経路詳細の表示エリア横幅も実質的に狭いままなので Vite テンプレ残りの #root 1126px 制限を撤去して shell--wide=1200px が有効になるようにする。具体的: (a) RouteSegment.operatorId 列を追加し既存値は line.operatorId 経由で migration 投入, (b) Backend API (POST/PUT/GET /api/routes) で operatorId を accept/return, (c) Frontend は API 受信時に segment.operator を pre-fill / 送信時に operatorId を含める, (d) RouteDetail で segment.operatorId からの操作会社名を tag-operator で表示, (e) frontend/src/index.css の #root を `width: 100%; align-items: center` に変更。
US-058	ユーザとして, 経路詳細 (S05) で各区間に運営会社を表示してほしいのと, 運営会社・種別・出発駅→到着駅を 1 行で表示してほしい (画面の横幅をもう少し広く取って良い)。具体的には: (a) RouteDetail の seg-body を column → row レイアウトに変更し, 運営会社タグ + 種別タグ + 路線名 + 出発→到着 を 1 行に並べる, (b) 運営会社は line.operatorName を引いて表示, (c) shell--wide の max-width を 1080 → 1200 px に拡張 (一覧画面の余裕も確保)。
US-057	ユーザとして, 通勤経路の登録/編集 (S04/S06) で各区間の運営会社の選択も必須にしたい。運営会社の選択肢に (指定なし) は残すが, 登録/更新時に (指定なし) はエラーとする。種別も必須のままで良いが, 選択肢に (指定なし) を含めて初期値は (指定なし) にする。登録/更新時の (指定なし) はエラー。路線名は引き続き任意。実装: (a) Segment 型の kind を `'' | LineKind` に拡張, (b) emptySegment の kind 初期値を '' に変更, (c) validate() に operator/kind の必須チェックを追加, (d) 種別 dropdown の先頭に <option value="">(指定なし)</option> を追加, (e) 運営会社 dropdown のラベルに [必須] を追記, (f) RouteEdit は API ロード後に line.operatorId から segment.operator を pre-fill する useEffect を追加 (snapshot も同期)。
US-056	管理者として, 愛知環状鉄道 (Aikan) のマスタ (運営会社 + 路線 + 23 駅) を手動登録したい。Wikidata 取り込み (US-011 / ADR 0007) のスコープ外 (JR_LINE_QIDS と OTHER_OPERATORS にいずれも含まれない) のため別途 idempotent な seed スクリプト `backend/scripts/seed-aikan.ts` を用意して投入する。Operator: aikan / 愛知環状鉄道 / aliases ["愛環"] / kinds ["train"]。Line: aikan-line / 愛知環状鉄道線 / kind=train。Station: 岡崎 → 高蔵寺 の 23 駅, 駅番号は公式な letter prefix 形式が未導入のため路線内順を 2 桁ゼロ詰め (01〜23) で暫定設定。岡崎・高蔵寺は US-049 operator-split ルールに従い JR 側の駅とは別レコード。
US-055	管理者として, 駅マスタ管理 (S10) の一覧で横スクロールが残る問題と, よみがな・運営会社・路線/駅番号でのソートが効かない問題を解消したい。具体的内容: (a) 駅マスタ管理一覧の ID 列を行番号 (#) 表示に置き換え, ID は行 hover の title 属性で参照可能にする (横スクロール解消), (b) 駅マスタ管理一覧に駅名 / よみがな / 運営会社 / 接続路線 のソート対応 (ヘッダクリックで昇/降順切替), (c) 路線マスタ管理一覧にも路線名 / 運営会社 / 種別 / 参照経路 / 接続駅 のソート対応 + ID 列を行番号に置換, (d) StationPicker (S07) で導入済の SortableTh コンポーネントを `frontend/src/components/SortableTh.tsx` に抽出して再利用。
US-054	管理者として, 運営会社マスタの新規/編集画面の「運営する種別」チェックボックス右ラベルが幅縮小時に 2 行折り返しされるのを防いで常に 1 行表示にしてほしい。また駅マスタ管理 (S10) の一覧で運営会社列のテキストが折り返されて表全体が横スクロールになり, 操作列の「編集」「削除」ボタンが切れるのを解消したい。具体的には: (a) AdminOperatorForm のチェックボックスラベルに `white-space: nowrap` を付与, (b) 駅マスタ管理 (S10) と路線マスタ管理 (S09) の運営会社列を路線/駅番号と同じ tag スタイル (small text + 背景色, .tag.tag-operator) で表示し 1 行に収める。
US-053	ユーザ・管理者として, 運営会社・種別・路線の cascade ルールを「上位 → 下位は narrow するが, 下位 → 上位は何もしない」非対称ルールに変更したい。なぜなら現状 (US-052) は下位選択時に上位が auto-select / hard-set されてしまい, ユーザが下位条件を試したい時に上位が勝手に固まってしまい, リセットボタン以外で個別に上位を「すべて」に戻す手段が無く不便だから。具体的内容: (a) cascade.ts の applyKind: line のみ narrow し operator は触らない, (b) cascade.ts の applyLine: 何もせず line だけセット, (c) visibleOperatorIds: kind / line で絞り込まず常に全 operator を返す。例外として route segment (RouteRegister/RouteEdit) は kind 必須のため line→kind hard-set だけ各画面で明示補完する (cascade ライブラリの責務外)。
US-052	管理者として, 運営会社マスタに「運営する種別 (train/subway/bus/other)」のチェックボックス UI を持たせたい。なぜなら現状は運営会社↔種別の紐付けが暗黙的 (Line.kind 経由) でしかなく, 路線マスタ管理 / 駅マスタ管理 / 駅マスタ参照 / 経路登録/編集の 種別 dropdown が運営会社選択時に絞り込まれないことが気になるから。具体的内容: (a) Operator.kinds JSON 配列カラムを追加し migration で既存 Line.kind から初期値を投入, (b) AdminOperators 一覧に「種別」列を表示, (c) AdminOperatorForm に種別チェックボックス UI を追加, (d) cascade.ts の visibleKinds と applyKind / applyOperator を Operator.kinds 駆動に変更し, 各画面で運営会社選択時に種別 dropdown を該当 kinds に絞り込む。種別 dropdown は line 選択を無視する (line 選択中も kind 切替可能 = applyKind が line をクリアする)。
US-051	ユーザ・管理者として, 細かな UI 微調整を 3 件適用したい。(a) プロフィール設定 (S08) の admin リンクは「運営会社マスタ管理 → 路線マスタ管理 → 駅マスタ管理」の上位 → 下位順に並べる。(b) 路線マスタ管理 (S09) と駅マスタ管理 (S10) の検索行右端 (件数表示の右) に「リセット」ボタンを設置し, 押すと運営会社・種別・路線フィルタが全て初期状態 (= すべて) に戻る。フィルタが全て空のときは disabled とする。(c) US-050 で実装済の cascade ルール (運営会社/路線/種別の上位 ↔ 下位連動) は本要望のとおり既に動作する旨を再確認した。

