# 駅マスタ管理画面 設計書

## 1. 画面概要

| 項目 | 内容 |
| --- | --- |
| 画面ID | S10 |
| 画面名 | 駅マスタ管理画面 |
| 概要 | 管理者が駅マスタ (Station) と接続路線 (StationLine) を一括で管理する画面 |
| URL | `/admin/stations` |
| アクセス権限 | 認証必須 + `User.role = "admin"` |
| 関連US | US-013, US-026, US-028, US-029, US-032, US-033, US-034, US-035, US-036 |
| 関連ADR | docs/adr/0006-master-admin.md (権限モデル + 参照整合性, §4-§6), docs/adr/0008-station-code-per-line.md (駅番号は路線ごとに保持) |

---

## 2. 画面レイアウト

```
┌──────────────────────────────────────────────────────────────┐
│  Admin / Stations                       [ + 新規作成 ]         │
│  駅マスタ管理                                                  │
│  登録済みの駅と接続路線を管理します (管理者専用)                  │
├──────────────────────────────────────────────────────────────┤
│  [通知バナー / エラーバナー (任意)]                              │
│                                                              │
│  ┌──────────┬──────┬──────────┬────────────────┬────────────┐│
│  │ ID         │ 駅名 │ よみがな  │ 接続路線        │ 操作       ││
│  ├──────────┼──────┼──────────┼────────────────┼────────────┤│
│  │ stn-gifu   │ 岐阜 │ ぎふ     │ [JR東海道線]    │[編集][削除]││
│  │ stn-nagoya │ 名古屋│ なごや   │ [JR東海道線][名鉄] │[編集][削除]││
│  └──────────────────────────────────────────────────────────┘│
│                                                              │
│  ─── New Station / Edit Station (フォーム) ───                │
│   ID  [_____________________]   ※ 空欄なら自動採番(cuid)     │
│   駅名 * [_______________________________]                   │
│   よみがな * [_______________________________]               │
│   接続路線:                                                   │
│     [□] 電車 JR東海道線                                       │
│     [□] 電車 名鉄名古屋本線                                   │
│     [□] 地下鉄 名古屋市営地下鉄名城線                          │
│   ※編集時は「駅名を変更しても既存経路文字列は追従しません」と注意 │
│                                                              │
│   [ 作成する / 更新する ]   [ キャンセル ]                     │
├──────────────────────────────────────────────────────────────┤
│  アカウント設定                            経路一覧へ            │
└──────────────────────────────────────────────────────────────┘
```

「+ 新規作成」「編集」は **専用画面に遷移する Link** (US-026)。新規は `/admin/stations/new`, 編集は `/admin/stations/:id/edit`。フォーム送信成功後は `/admin/stations` に navigate state.notice 付きで戻り、一覧に通知バナーが表示される。
専用画面では路線一覧を `/api/lines` から取得し、各路線を **「チェックボックス + 路線名 + 駅番号 input」** の 1 行レイアウトで表示する (US-033)。チェックボックス列は本体程度の最小幅にし、駅番号入力枠を同じ行に並べる。チェックを外すと駅番号 input は値クリアされる。

US-032: 駅マスタ管理 (S10) の一覧上部に「種別」「路線」フィルタを設置する (US-017 と同じ cascade: 種別を選ぶと路線セレクトが該当 kind に絞られる)。フィルタは AND 条件 (路線指定 = その路線に接続している駅のみ)。

US-034: フィルタの選択値は sessionStorage (key: `admin-stations-filter`) に保存し, 新規/編集画面から戻ってきたときに復元する。一覧 fetch は mount 時 useEffect で実行されるため, 復元と同時に最新状態が表示される。

US-035: 「接続路線 / 駅番号」入力リスト (.line-picker) の grid 列幅を `auto / auto / minmax(0,1fr) / 9em` とし, 路線名は `white-space: nowrap` で 1 行強制 + 必要なら `text-overflow: ellipsis` で省略, 駅番号 input は 9em (CA68 等の短いコードに十分) に制限する。

US-036: 一覧表の駅名 / よみがなセルは `white-space: nowrap` で 1 行表示。接続路線セルは `tag-row` 内で `flex-wrap: wrap` を許容し, タグ数が増えたときだけ高さが広がるよう調整する。

---

## 3. 入力項目定義

| No | 項目名 | 表示ラベル | 入力形式 | 必須 | 桁数 | バリデーション | 備考 |
| --- | --- | --- | --- | :-: | --- | --- | --- |
| 1 | ID | ID | テキスト | × | 1〜80文字 | `^[A-Za-z0-9._-]+$` (指定時) | 空欄なら cuid 自動採番。編集時は disabled |
| 2 | 駅名 | 駅名 | テキスト | ◯ | 1〜50文字 | — | RouteSegment は文字列複製のため、改名しても既存経路は追従しない |
| 3 | よみがな | よみがな | テキスト | ◯ | 1〜80文字 | — | `/api/stations` の検索 (q) で部分一致対象 |
| 4 | 接続路線 | 接続路線 | チェックボックス群 | × | 0〜50件 | 存在する Line.id のみ | 不存在 id は 400 (unknown_line) を返す。チェックされた行のみ駅番号 input を有効化 (US-033) |
| 5 | 駅番号 (路線ごと) | 駅番号 | テキスト | × | 0〜30文字 | `^[A-Za-z0-9/-]*$` | 各接続路線の右側に表示。チェック ON のときだけ送信対象。空文字許容。Wikidata の P296 由来値 (例: `CA68`) や手動採番値 (US-033) |

---

## 4. ボタン定義

| No | ボタン名 | 表示ラベル | 動作 |
| --- | --- | --- | --- |
| 1 | 新規作成 (ヘッダ / 空状態) | + 新規作成 | フォームを `create` モードで開き入力欄をクリア |
| 2 | 編集 (行内) | 編集 | フォームを `edit` モードで開き既存値を pre-fill (ID は disabled) |
| 3 | 削除 (行内) | 削除 | `window.confirm` で意思確認 → `DELETE /api/admin/stations/:id`。削除は無制約 (ADR 0006 §4) |
| 4 | 作成する / 更新する | 作成する / 更新する | `POST /api/admin/stations` または `PUT /api/admin/stations/:id` を呼出 |
| 5 | キャンセル | キャンセル | フォームを閉じる |
| 6 | 路線マスタ管理 (フォーム内のヒント) | 路線マスタ管理 | 路線マスタが空のときだけ表示。`/admin/lines` へ遷移 |
| 7 | アカウント設定 (foot) | アカウント設定 | `/account` へ遷移 |
| 8 | 経路一覧へ (foot) | 経路一覧へ | `/routes` へ遷移 |

---

## 5. 画面遷移

| イベント | 遷移先 | 条件 |
| --- | --- | --- |
| 一般ユーザがアクセス | 本画面 (403 表示) | role !== 'admin' |
| 未認証アクセス | `/login` | セッション無し |
| GET /api/users/me 401 | `/login` | セッション失効 |
| 作成 / 更新成功 | 本画面 (フォーム閉じる + 通知バナー) | API 200/201 |
| 削除成功 | 本画面 (リスト reload + 通知バナー) | API 204 |

---

## 6. バリデーション仕様

### 6.1 クライアントサイド

- ID (作成時, 任意): 空欄なら検証スキップ。指定する場合 `^[A-Za-z0-9._-]+$` + 80文字以内
- 駅名: 未入力 / 50文字超過
- よみがな: 未入力 / 80文字超過
- 接続路線: 0件許容
- 「作成する/更新する」押下時に検証。NG ならフォーム内バナー表示、API 呼出は行わない

### 6.2 サーバサイド

- zod (`StationCreateInput` / `StationUpdate`) で同等チェック + 認証 + role=admin 検証
- 失敗時 400 (validation_failed / unknown_line) / 401 / 403 / 404 / 409 (duplicate)

### 6.3 エラーメッセージ (フォームバナー)

| 条件 | メッセージ |
| --- | --- |
| 駅名未入力 | 駅名を入力してください |
| 駅名 51文字以上 | 駅名は50文字以内で入力してください |
| よみがな未入力 | よみがなを入力してください |
| よみがな 81文字以上 | よみがなは80文字以内で入力してください |
| ID 形式違反 | IDは半角英数字 + ハイフン/ドット/アンダースコアのみ使用できます |
| 401 | (画面外で /login へリダイレクト) |
| 403 | 管理者権限が必要です |
| 400 (validation_failed) | 入力内容に誤りがあります |
| 400 (unknown_line) | 紐付けに含まれる路線が存在しません (削除済み?) |
| 404 | 編集対象の駅が見つかりませんでした (削除済み?) |
| 409 | 同じIDの駅が既に登録されています |
| 5xx / 通信 | 保存に失敗しました。再度お試しください |

---

## 7. その他考慮事項

- 駅一覧は **kana 昇順** で表示
- 削除に参照件数チェックは行わない (RouteSegment.fromStation/toStation は文字列複製で外部キー無いため壊れない)
- 駅名変更時の注意書きを編集モードで常時表示 (ADR 0006 §5)
- 路線↔駅の M:N は **本画面のチェックボックス + 駅番号 input で一括反映** (専用の station-line 個別 API は提供しない。ADR 0006 §6)
  - サーバ側は `StationLine` 全置換 (`deleteMany + createMany` 等価) で実装。各 `StationLine` に `code` (駅番号) を保持 (ADR 0008)
- 路線マスタが空 (`/api/lines` が 0 件) のとき、フォームの接続路線セクションに `/admin/lines` への誘導リンクを表示
- 一覧の絞り込み: 種別 + 路線フィルタを設置 (US-032)。サーバ側は `lineLinks.some.line.kind / id` で絞る

---

## 8. 未確定事項

- 一覧の検索 / 並び替え UI (件数が 1,000+ 規模になったとき)
- 駅名変更時に既存 RouteSegment 文字列を一括置換するメニューの要否 (ADR 0006 §将来検討余地)
- 監査ログ (操作履歴) (ADR 0006 §7 では未対応)
- 楽観ロック (現状は Last Write Wins)
